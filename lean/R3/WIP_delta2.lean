import R3.DeltaTwo
/-!
# Scratch file for the delta = 2 lane.  NOT imported by `R3.lean`.

Keep unfinished proofs here.  The gate laundering scan greps all of `R3/`, including this
file, so it must stay free of every banned construct listed in `gate.sh` — comment unfinished
proofs out instead of parking a placeholder in them.
-/
namespace R3
open Finset

end R3
