import R3.LinkTypes
/-!
# WIP — NOT imported by R3.lean.  Scratch space for F(11).

Empty: everything developed here is proved and has moved to `R3/LinkTypes.lean`
(`link_types`, the L1..L4 classification, included).

Next target: the `delta = 4` case of `R3_equals_10.md` at `n = 11` — see `PLAN_F11.md`
Step 1 and `HANDOFF.md`.
-/
namespace R3
open Finset

end R3
